# Важно перед началом использования
Перед написанием собственного JavaScript-кода рекомендуется ознакомиться с информацией ниже — это поможет избежать ошибок и некорректной работы скриптов.
---

### 🧩 Обёртка кода

Перед написанием логики **обязательно оборачивайте код** в один из обработчиков событий, в зависимости от того, **когда именно он должен выполняться**.

Ниже представлены основные варианты.

```js
// Код будет выполнен после загрузки DOM-дерева
document.addEventListener("DOMContentLoaded", () => {
    console.log("DOM загружен");
});

// Код будет выполнен при переходе между страницами магазина (Inertia)
document.addEventListener("inertia:navigate", () => {
    console.log("Переход на другую страницу");
});

// Код будет выполнен после полной загрузки страницы
// Именно сюда рекомендуется помещать основную логику
document.addEventListener("inertia:finish", () => {
    console.log("Страница полностью загружена");
});
```

| Событие            | Когда срабатывает              | Рекомендация                 |
| ------------------ | ------------------------------ | ---------------------------- |
| `DOMContentLoaded` | Когда загружен DOM             | Для базовой инициализации    |
| `inertia:navigate` | При переходе между страницами  | Для отслеживания навигации   |
| `inertia:finish`   | После полной загрузки страницы | **Основная логика скриптов** |

👀 Observer (MutationObserver)
Так как страницы магазина не являются статичными, элементы могут:

- появляться динамически
- перерисовываться без полной перезагрузки страницы

В таких случаях рекомендуется использовать MutationObserver.

⚠️ Используйте Observer только если понимаете, что и зачем отслеживаете — неправильная реализация может привести к утечкам памяти и снижению производительности.

Пример базового использования:
```js
const observer = new MutationObserver((mutations) => {
    mutations.forEach(() => {
        console.log("DOM был изменён");
    });
});

observer.observe(document.body, {
    childList: true,
    subtree: true
});
```